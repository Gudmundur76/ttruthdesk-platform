# agentgateway v1.2.1 — citation.is MCP proxy
# SSE endpoint: https://citation.manus.space/mcp (returns text/event-stream)
# JSON-RPC endpoint: https://citation.manus.space/api/mcp (returns application/json)
binds:
- port: 3000
  listeners:
  - routes:
    - backends:
      - mcp:
          targets:
          - name: citation-is-mcp
            sse:
              host: citation.manus.space
              port: 443
              path: /mcp
            policies:
              backendTLS:
                hostname: citation.manus.space
      policies:
        cors:
          allowOrigins:
          - "*"
          allowHeaders:
          - mcp-protocol-version
          - content-type
          - cache-control
          - mcp-session-id
          exposeHeaders:
          - "Mcp-Session-Id"
